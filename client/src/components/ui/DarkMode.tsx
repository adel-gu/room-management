import { SunMoon } from 'lucide-react';
import ButtonIcon from './ButtonIcon';

const DarkMode = () => {
  return (
    <ButtonIcon>
      <SunMoon />
    </ButtonIcon>
  );
};
export default DarkMode;
